import react from 'react';

export const HeroSection = () => {
  return (
    <section className={`relative overflow-hidden mt-24 bg-cover bg-center h-[75vh]`} style={{background: "url('/images/hero_v2_bg.png')"}}>
      This is the Hero Section
    </section>
  );
};